# 项目状态（Status）

**更新日期**：2026-09-23｜**开发分支**：`develop`。源码进入 `main` 须经 PR、`Plugins / Required` 检查与 squash merge；预览和稳定发行按[发行指南](RELEASING.md)分别授权执行。

> 本文件记录尚未完成的开发计划、活跃技术验证和当前未解决问题，不重复已完成事项。当前实现和支持范围以代码、测试及[专项适配器说明](TASK_ADAPTERS.md)为准；发布事实以发行记录为准。本地测试或 develop 推送不代表插件已正式发布。正式版本仅按用户明确指定修改。

## 当前未完成事项

专项任务协议首发 `0.1.0` 对应 Host `0.16.8`，专项插件 `minHostVersion` 保持 `0.16.8`；okww、oknte 初始版本为 `0.1.0`。旧开发二进制不因版本号相同自动具备新能力。以下待办区分缺少证据的业务分支与尚未验证的真实环境；不能将明确受限的组合登记为已支持。

## 后续功能：专项适配与配置诊断

- [ ] 继续扩展八个专项适配器的内部业务终态与反例。BetterGI 自定义脚本、BAAH 静默领取/点击和其他缺少明确确认日志的路径仍保留 unknown；技术包装器结束、外层完成或配置开关不能替代业务成功证据。
- [ ] 解决 MaaEnd / MaaStellaSora（MXU）的匿名异步 focus 归属。现有上游输出缺少原任务 ID，不得按最近任务绑定；需要可核实的身份链后才能扩展内部节点判定。MAAS 的其他 launcher 样本不得作为 MXU 正例。
- [ ] 继续核实三月七静默领奖、prepare 未确认等分支的可观测性。源码日志桥原型默认关闭、不进入生产 ZIP；官方 v2026.9.7 隔离 `--help` 试验未加载所测试的旁置钩子，其他官方接入方式仍待独立验证，不将源码原型称为官方支持。
- [ ] 扩展 OK 两渠道的版本支持与更新/repair 身份验证。当前锁定 ok-ww v3.6.7、ok-nte v1.3.19；Global 异环实际自动升级至 v1.3.20 的组合保持阻断。固定的工作文件校验不覆盖完整解释器/依赖，也不能排除瞬时替换；新版本不能通过删除守卫放行。
- [ ] 按已核实的真实字段、默认值、有效配置优先级及调用点继续扩展配置诊断。新增路径或收尾规则须覆盖当前绑定、云模式、PC/ADB 类型、参数覆盖和后续队列；未查明字段不猜测，不自动改写用户游戏配置。普通路径不一致通常只提醒。
- [ ] 继续补充可追溯的真实日志与独立留出样本。现有开发夹具包含明确标注的合成/源码派生输入；单个 BGI 留出会话不是八产品准确率验证，公开附件下载成功也不证明版本、launcher 或完整业务终态。

## 活跃技术验证与决策

- [ ] 在另行授权的隔离真实账号/游戏环境执行八款专项业务验证，逐次记录版本、渠道、实际任务和结果。官方启动器参数转发、原始源码分支、Jint、配置事务和 Test Host 测试不能替代真实游戏运行。
- [ ] 在真实设备上完成 `EmulatorSupport` 的雷电、夜神、BlueStacks 验证：实例身份与 ADB 端点映射、应用启动、前台查询、截图、应用停止和安全关闭。具体矩阵由[发行指南](RELEASING.md)维护，宿主 provider fixture 不算真机通过。
- [ ] 为更多 managed-code 插件补充 mock HTTP、事件及失败恢复回归；持续验证用户级数据清理、密钥保护与升级后的当前数据访问，不把宿主边界测试当作所有插件业务已验证。
- [ ] 继续完善插件设置贡献的字段校验、错误呈现与目录元数据检查，并覆盖配置变更后的重新评估和不同用户之间的隔离。

## 发行与兼容过渡

- 任何新的产品源码修改都需要从受保护 main 重新生成并验收候选；develop 推送不代替 main 合并、预览发布或稳定发布。
- [ ] 在 T1 首次正式发行后登记实际版本与日期，经历完整兼容周期后再确定 T2。T2 才移除 Host 旧 `configValidator` runner、调用点及公共契约，并验证旧插件的可操作升级提示；本轮保留旧安装包兼容，不提前删除。

## 维护规则

- 完成、失效或确认为既定契约的事项及时移出本台账，历史由 Git 与发行记录保留；不要长期累积已完成检查清单。
- 新条目先确认当前代码路径、测试和影响范围；区分“尚未运行”“已验证但受限”和“实现缺失”，不把 unknown 写成成功。
- 真实环境与新的 UAC 试验分别依据实际授权执行，既有隔离试验许可不自动扩展到其他现场。
- 修改公开协议、资源布局、诊断或运行语义时同步 Host 契约与两仓测试；同一正式插件版本的稳定包字节不可变。
- 文档与代码/测试冲突时先核对实际契约，不通过修改台账掩盖失败或扩大支持范围。具体作者与验证入口见[贡献指南](../CONTRIBUTING.md)和[文档门户](README.md)。
