{
  "require": [
    { "var": "main", "file": "mxu.exe" }
  ],
  "paths": {
    "mainExe": "{main}",
    "args": "--autostart --quit-after-run",
    "configPath": "config",
    "logPath": "debug/{YYYY-MM-DD}-*.log"
  }
}
