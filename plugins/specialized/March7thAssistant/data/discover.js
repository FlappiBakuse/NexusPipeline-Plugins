// Generated by tools/generate_task_protocol.py. Edit tools/task-protocol sources.
const ADAPTER = {"id":"march7th","implementation":"march7th","defaults":{"log_overlay_enable":true,"autoplot_skip_enable":true,"autoplot_click_enable":true,"autoplot_battle_detect_enable":true,"autoplot_phone_detect_enable":true,"debug_mode_enable":false,"check_update":true,"pause_after_success":true,"exit_after_failure":false,"play_audio":false,"update_via_launcher":false,"cloud_game_enable":false,"cloud_game_fullscreen_enable":true,"cloud_game_use_paid_time":false,"browser_headless_enable":false,"browser_headless_restart_on_not_logged_in":true,"browser_dump_cookies_enable":false,"browser_persistent_enable":true,"browser_download_use_mirror":true,"power_enable":true,"power_plan_keep":false,"tp_before_instance":false,"build_target_enable":false,"build_target_use_user_instance_when_only_erosion_and_ornament":false,"break_down_level_four_relicset":false,"instance_team_enable":false,"merge_immersifier":false,"use_reserved_trailblaze_power":false,"use_fuel":false,"echo_of_war_enable":false,"borrow_enable":true,"borrow_character_enable":false,"reward_enable":true,"reward_dispatch_enable":true,"reward_mail_enable":true,"reward_assist_enable":true,"reward_quest_enable":true,"reward_srpass_enable":true,"reward_redemption_code_enable":true,"reward_achievement_enable":false,"reward_message_enable":false,"daily_enable":true,"daily_material_enable":true,"daily_himeko_try_enable":false,"daily_memory_one_enable":false,"activity_enable":true,"activity_dailycheckin_enable":true,"activity_gardenofplenty_enable":false,"activity_realmofthestrange_enable":false,"activity_planarfissure_enable":false,"activity_journey_highlights_notification_enable":false,"asset_manager_enable":false,"asset_self_molding_resin_enable":false,"asset_ember_special_pass_enable":false,"asset_ember_regular_pass_enable":false,"asset_ember_tracks_of_destiny_enable":false,"asset_lc3_star_superimpose_enable":false,"currencywars_enable":false,"currencywars_bonus_enable":false,"currencywars_fast_mode":true,"currencywars_strategy_restart_on_special_tags":true,"weekly_divergent_enable":false,"weekly_divergent_bonus_enable":false,"weekly_divergent_stable_mode":false,"divergent_station_priority_enable":false,"universe_enable":false,"universe_enable_gpu":false,"universe_bonus_enable":false,"universe_requirements":false,"fight_enable":false,"fight_team_enable":false,"fight_requirements":false,"genshin_starRail_fps_unlocker_allow":false,"forgottenhall_enable":false,"purefiction_enable":false,"apocalyptic_enable":false,"auto_battle_detect_enable":true,"auto_set_resolution_enable":true,"auto_set_game_path_enable":true,"hotkey_toggle_autoplot_enable":false,"scheduled_run_enable":false,"scheduled_chain_continue_on_failure":true,"use_background_screenshot":true,"window_maximized":false,"update_prerelease_enable":false,"update_full_enable":true,"auto_update":false,"notification_enable":true,"notify_merge":false,"notify_send_images":true,"notify_winotify_enable":true,"notify_telegram_enable":false,"notify_matrix_enable":false,"notify_matrix_separately_text_media":true,"notify_serverchanturbo_enable":false,"notify_serverchan3_enable":false,"notify_bark_enable":false,"notify_smtp_enable":false,"notify_smtp_ssl":true,"notify_smtp_starttls":false,"notify_smtp_ssl_unverified":false,"notify_smtp_plain_text":false,"notify_onebot_enable":false,"notify_gocqhttp_enable":false,"notify_dingtalk_enable":false,"notify_pushplus_enable":false,"notify_wechatworkapp_enable":false,"notify_wechatworkbot_enable":false,"notify_gotify_enable":false,"notify_discord_enable":false,"notify_pushdeer_enable":false,"notify_lark_enable":false,"notify_lark_imageenable":false,"notify_kook_enable":false,"notify_meow_enable":false,"notify_webhook_enable":false,"notify_custom_enable":false,"telemetry_enable":true},"names":{"echo_of_war_enable":"历战余响","reward_assist_enable":"支援奖励","reward_mail_enable":"邮件奖励","reward_dispatch_enable":"委托奖励","reward_quest_enable":"实训奖励","reward_srpass_enable":"无名勋礼","reward_achievement_enable":"成就奖励","reward_message_enable":"短信奖励","reward_redemption_code_enable":"兑换码","asset_self_molding_resin_enable":"自塑尘脂合成","asset_lc3_star_superimpose_enable":"3星光锥叠加","asset_ember_special_pass_enable":"星轨专票兑换","asset_ember_regular_pass_enable":"星轨通票兑换","asset_ember_tracks_of_destiny_enable":"命运的足迹兑换","activity_dailycheckin_enable":"活动签到","activity_gardenofplenty_enable":"花藏繁生","activity_realmofthestrange_enable":"异器盈界","activity_planarfissure_enable":"位面分裂","activity_journey_highlights_notification_enable":"旅途拾忆通知"}};
// Bundled by generate_task_protocol.py; no Node/CLR or runtime imports.
'use strict';
function requireValue(ok) { if (!ok) throw new Error('unsupported_schema'); }
function object(v) { return v !== null && typeof v === 'object' && !Array.isArray(v); }
function unique(values) { return new Set(values).size === values.length; }
function clean(text) { return String(text).replace(/\x1b\[[0-9;]*m/g, '').trim(); }
function configOne(predicate) {
  const found = input.configResources.filter(predicate || (() => true)); requireValue(found.length === 1);
  return { id: found[0].id, ...nexus.readConfig(found[0].id) };
}
function jsonc(text) {
  // Native token matching avoids a per-character Jint statement cost on full MXU interfaces.
  // Quoted strings are consumed first, preserving URL/comment-like and comma-like string content.
  try { return JSON.parse(text); } catch { /* upstream interfaces also allow JSONC */ }
  const stripped = text.replace(/"(?:\\.|[^"\\])*"|\/\/[^\r\n]*|\/\*[\s\S]*?\*\//g,
    token => token[0] === '"' ? token : ' ');
  return JSON.parse(stripped.replace(/"(?:\\.|[^"\\])*"|,\s*(?=[}\]])/g,
    token => token[0] === '"' ? token : ''));
}
const resourceCache = {};
function resource(id) {
  if (!Object.prototype.hasOwnProperty.call(resourceCache, id)) {
    const r = nexus.readResource(id); resourceCache[id] = r.format === 'text' ? jsonc(r.document) : r.document;
  }
  return resourceCache[id];
}
function endRun(result, line, ruleId, aborted) {
  if (result.runBoundary === 'aborted' && !aborted) return;
  result.runBoundary = aborted ? 'aborted' : 'ended';
  result.boundaryEvidence = [{ sourceId: line.sourceId, epoch: line.epoch, sequence: line.sequence, ruleId }];
}
function createPlan() { return { protocolVersion: '1.0', type: 'discovery', coverage: 'partial', tasks: [], selectionFields: [], behaviorFields: [], diagnostics: [], slots: {} }; }
function behavior(plan, config, keys, prefix) {
  for (const key of keys) if (Object.prototype.hasOwnProperty.call(config.document, key))
    plan.behaviorFields.push({ resourceId: config.id, selector: (prefix || []).concat(key) });
}
function addTask(plan, resourceId, key, name, enabled, selector, risk, detection, role, parentId) {
  const id = ADAPTER.id + ':' + key;
  requireValue(typeof enabled === 'boolean' && typeof name === 'string' && name.length > 0 && !plan.tasks.some(t => t.id === id));
  const task = { id, sourceKey: key, name, parentId: parentId || null, role: role || 'business', enabled,
    order: plan.tasks.length, countsAsUnit: !parentId && (!role || role === 'business'), requiredForParent: true,
    retryUnitId: parentId || id, retryRisk: risk || 'unknown', dependencies: [], detection: detection || 'limited', configRef: resourceId };
  plan.tasks.push(task);
  if (selector) {
    const field = { resourceId, selector, purpose: 'selection' };
    if (!plan.selectionFields.some(f => JSON.stringify(f) === JSON.stringify(field))) plan.selectionFields.push(field);
    plan.slots[id] = field;
  }
  return task;
}
function select(document, selector) {
  let node = document;
  for (const token of selector) {
    if (typeof token === 'string') { requireValue(object(node) && Object.prototype.hasOwnProperty.call(node, token)); node = node[token]; }
    else if (Object.prototype.hasOwnProperty.call(token, 'by')) {
      requireValue(Array.isArray(node)); const found = node.filter(v => object(v) && JSON.stringify(v[token.by]) === JSON.stringify(token.value));
      requireValue(found.length === 1); node = found[0];
    } else {
      requireValue(Array.isArray(node) && object(node[token.index]) && JSON.stringify(node[token.index][token.guardKey]) === JSON.stringify(token.guardValue)); node = node[token.index];
    }
  }
  return node;
}
function retryPlan(discover) {
  const stop = reason => ({ protocolVersion: '1.0', type: 'retry', decision: 'stop', reasonCode: reason,
    includedTaskIds: [], prerequisiteTaskIds: [], expandedUnitIds: [], filePatches: [] });
  if (input.cancelled || input.budgetExhausted || input.attemptsUsed >= input.maxAttempts) return stop('retry.budget_exhausted');
  const tasks = input.originalPlan.tasks, selected = new Set(tasks.filter(t => t.enabled && ['failed', 'blocked'].includes(input.taskStates[t.id])).map(t => t.id));
  if (!selected.size) return stop('retry.no_verified_candidate');
  const prerequisites = new Set(), expanded = new Set();
  let changed = true;
  while (changed) {
    const count = selected.size;
    for (const id of Array.from(selected)) {
      const task = tasks.find(t => t.id === id); requireValue(task);
      const unit = tasks.filter(t => t.enabled && t.retryUnitId === task.retryUnitId).map(t => t.id).concat(task.retryUnitId);
      if (unit.some(id => !selected.has(id))) expanded.add(task.retryUnitId);
      unit.forEach(id => selected.add(id)); task.dependencies.forEach(id => { selected.add(id); prerequisites.add(id); });
    }
    changed = selected.size !== count;
  }
  if (tasks.some(t => selected.has(t.id) && (t.retryRisk !== 'safe' || (!t.enabled && t.role === 'business')))) return stop('retry.risk_not_verified');
  const included = [], remaining = new Set(selected);
  while (remaining.size) {
    const ready = tasks.filter(t => remaining.has(t.id) && !t.dependencies.some(d => remaining.has(d))).sort((a, b) => a.order - b.order || a.id.localeCompare(b.id));
    requireValue(ready.length > 0); ready.forEach(t => { included.push(t.id); remaining.delete(t.id); });
  }
  const current = discover(), patches = {};
  if (typeof customRetryPatches === 'function') {
    const custom = customRetryPatches(current, selected);
    return { protocolVersion: '1.0', type: 'retry', decision: 'selective', reasonCode: 'retry.unfinished', includedTaskIds: included,
      prerequisiteTaskIds: included.filter(id => prerequisites.has(id)), expandedUnitIds: Array.from(expanded).sort(), filePatches: custom };
  }
  for (const task of tasks) {
    const slot = current.slots[task.id];
    if (!slot) { if (selected.has(task.id) && !task.enabled) return stop('retry.no_selection_field'); continue; }
    const r = nexus.readConfig(slot.resourceId), expected = select(r.document, slot.selector);
    if (typeof expected !== 'boolean') return stop('retry.unsupported_selection');
    const value = selected.has(task.id);
    if (expected === value) continue;
    if (!patches[slot.resourceId]) patches[slot.resourceId] = { resourceId: slot.resourceId, format: r.format, expectedRevision: r.revision, operations: [] };
    patches[slot.resourceId].operations.push({ selector: slot.selector, expected, value, purpose: 'selection' });
  }
  return { protocolVersion: '1.0', type: 'retry', decision: 'selective', reasonCode: 'retry.unfinished', includedTaskIds: included,
    prerequisiteTaskIds: included.filter(id => prerequisites.has(id)), expandedUnitIds: Array.from(expanded).sort(), filePatches: Object.values(patches) };
}
function observeRules(rule) {
  const result = { protocolVersion: '1.0', type: 'observation', runId: input.runId, attemptId: input.attemptId,
    runBoundary: 'open', boundaryEvidence: [], diagnostics: [], observations: [], cursorState: input.adapterState || {} };
  const tasks = input.originalPlan.tasks.filter(t => input.attemptTaskIds.includes(t.id));
  const states = JSON.parse(JSON.stringify(input.acceptedState));
  function emit(task, status, line, ruleId, skipKind) {
    const old = states[task.id] || { status: 'pending', executionOrdinal: 0 };
    let ordinal = old.executionOrdinal || 1;
    if (status === 'running' && old.executionOrdinal > 0 && !['running', 'pending'].includes(old.status)) ordinal++;
    const observation = { id: [line.sourceId, line.epoch, line.sequence, task.id, status].join(':'), taskId: task.id,
      executionOrdinal: ordinal, status, reasonCode: ruleId, evidence: [{ sourceId: line.sourceId, epoch: line.epoch, sequence: line.sequence, ruleId }] };
    if (skipKind) observation.skipKind = skipKind;
    result.observations.push(observation); states[task.id] = { executionOrdinal: ordinal, status };
  }
  for (const line of input.logBatch.records) rule(clean(line.text), tasks, (task, status, ruleId, skipKind) => emit(task, status, line, ruleId, skipKind), result, line, states);
  return result;
}
function dispatch() {
  try {
    if (input.phase === 'discover') { const result = discover(); delete result.slots; return result; }
    if (input.phase === 'observe') return observeRules(observe);
    if (input.phase === 'retry') return retryPlan(discover);
    throw new Error('protocol_error');
  } catch (error) {
    if (input.phase !== 'discover') throw error;
    return { protocolVersion: '1.0', type: 'discovery', coverage: 'unsupported', tasks: [], selectionFields: [],
      diagnostics: [{ code: 'unsupported_schema', message: 'Configuration identity or schema could not be verified.' }] };
  }
}

function discover() {
  const config = configOne(r => r.id === 'config:config.yaml'), d = config.document, plan = createPlan();
  requireValue(object(d));
  const defaults = ADAPTER.defaults;
  behavior(plan, config, Object.keys(d).filter(k => /^(power_|universe_|weekly_|currencywars_|fight_|forgottenhall_|purefiction_|apocalyptic_|asset_|activity_|echo_)/.test(k)
    && !/(?:_time|_timestamp|_last_run|_completed_count|_progress)$/.test(k) && !k.endsWith('_enable')));
  const value = key => Object.prototype.hasOwnProperty.call(d, key) ? d[key] : defaults[key];
  const roots = [
    ['daily_enable', '每日实训'], ['power_enable', '清体力'], ['universe_enable', '模拟宇宙/差分宇宙'],
    ['weekly_divergent_enable', '差分宇宙积分奖励'], ['currencywars_enable', '货币战争积分奖励'], ['fight_enable', '锄大地'],
    ['forgottenhall_enable', '忘却之庭'], ['purefiction_enable', '虚构叙事'], ['apocalyptic_enable', '末日幻影'],
    ['asset_manager_enable', '资产管理'], ['reward_enable', '领取奖励'], ['activity_enable', '活动'], ['build_target_enable', '培养目标']
  ];
  for (const [key, name] of roots) {
    requireValue(typeof value(key) === 'boolean');
    const task = addTask(plan, config.id, key, name, value(key), typeof d[key] === 'boolean' ? [key] : null,
      'unknown', 'limited', key === 'build_target_enable' ? 'technical' : 'business');
    const childKeys = key === 'power_enable' ? ['echo_of_war_enable']
      : key === 'reward_enable' ? ['reward_assist_enable', 'reward_mail_enable', 'reward_dispatch_enable', 'reward_quest_enable', 'reward_srpass_enable', 'reward_achievement_enable', 'reward_message_enable', 'reward_redemption_code_enable']
      : key === 'asset_manager_enable' ? ['asset_self_molding_resin_enable', 'asset_lc3_star_superimpose_enable', 'asset_ember_special_pass_enable', 'asset_ember_regular_pass_enable', 'asset_ember_tracks_of_destiny_enable']
      : key === 'activity_enable' ? ['activity_dailycheckin_enable', 'activity_gardenofplenty_enable', 'activity_realmofthestrange_enable', 'activity_planarfissure_enable', 'activity_journey_highlights_notification_enable'] : [];
    for (const child of childKeys) {
      if (typeof value(child) !== 'boolean') continue;
      addTask(plan, config.id, child, ADAPTER.names[child] || child, task.enabled && value(child),
        typeof d[child] === 'boolean' ? [child] : null, 'unknown', 'limited', undefined, task.id);
    }
  }
  for (const key of Object.keys(d).filter(k => k.endsWith('_enable') && !Object.prototype.hasOwnProperty.call(defaults, k)))
    if (!plan.tasks.some(t => t.sourceKey === key)) addTask(plan, config.id, key, key, d[key] === true, null, 'unknown', 'unsupported');
  plan.diagnostics.push({ code: 'coverage_limited', message: 'Reward wrapper completion and timestamps are not success evidence. Internal randomized daily tasks do not expand the frozen plan.' });
  return plan;
}
function observe(text, tasks, emit, result, line, states) {
  // tasks/game.stop logs this top-level header before optional game shutdown and pause/loop.
  // The generic dashed "完成" banner also ends startup and nested modules, so it is never a run boundary.
  if (/^\|\s*停止运行\s*\|$/.test(text)) endRun(result, line, 'march7th.run.stop');
  if (/^(?:\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2},\d{3} \| ERROR \| )?发生错误(?:\s|:|：|$)/.test(text))
    endRun(result, line, 'march7th.run.exception', true);
  const absent = text.match(/(?:^|\| INFO \| )未检测到(邮件|支援|委托|每日实训|无名勋礼|成就|短信)奖励$/);
  const rewards = { 邮件: 'reward_mail_enable', 支援: 'reward_assist_enable', 委托: 'reward_dispatch_enable',
    每日实训: 'reward_quest_enable', 无名勋礼: 'reward_srpass_enable', 成就: 'reward_achievement_enable', 短信: 'reward_message_enable' };
  if (absent) {
    const task = tasks.find(t => t.sourceKey === rewards[absent[1]]);
    if (task) emit(task, 'succeeded', 'march7th.reward.not_available');
  }
  // RewardTemplate emits a named completion even when nothing is available to claim.
  const complete = text.match(/^(?:[-─━]+\s*)?(邮件|支援|委托|每日实训|无名勋礼|成就|短信)奖励完成(?:\s*[-─━]+)?$/);
  if (complete) {
    const task = tasks.find(t => t.sourceKey === rewards[complete[1]]);
    if (task) emit(task, 'succeeded', 'march7th.reward.completed');
  }
  const rules = [
    ['reward_mail_enable', /邮件奖励已领取\s*$/, 'succeeded', 'mail.claimed'],
    ['reward_dispatch_enable', /委托派遣中，目前没有可领取的奖励帕！\s*$/, 'succeeded', 'dispatch.in_progress'],
    ['daily_enable', /实训分数已达标\s*$/, 'succeeded', 'daily.target'],
    ['universe_enable', /差分宇宙固定次数已全部完成，记录本轮执行时间\s*$/, 'succeeded', 'universe.complete'],
    ['universe_enable', /差分宇宙本轮未完成，不计入固定次数/, 'failed', 'universe.incomplete'],
    ['universe_enable', /差分宇宙固定次数为 0，跳过执行并记录本轮时间\s*$/, 'skipped', 'universe.zero'],
    ['universe_enable', /差分宇宙固定次数（.+）已完成 \d+\/\d+，跳过执行\s*$/, 'skipped', 'universe.satisfied'],
    ['weekly_divergent_enable', /尝试运行了多轮差分宇宙，但积分奖励仍未完成/, 'failed', 'weekly.incomplete'],
    ['activity_enable', /活动处理过程中发生错误:/, 'failed', 'activity.exception'],
    ['echo_of_war_enable', /历战余响设置周\d+后开始执行，当前为周\d+, 跳过执行\s*$/, 'skipped', 'echo.not_due']
  ];
  const refresh = { daily_enable: '每日实训', echo_of_war_enable: '历战余响', universe_enable: '模拟宇宙/差分宇宙',
    weekly_divergent_enable: '「差分宇宙」积分奖励', currencywars_enable: '「货币战争」积分奖励', fight_enable: '锄大地',
    forgottenhall_enable: '忘却之庭', purefiction_enable: '虚构叙事', apocalyptic_enable: '末日幻影', asset_self_molding_resin_enable: '自塑尘脂自动合成' };
  for (const task of tasks) {
    if (refresh[task.sourceKey] && text.endsWith(refresh[task.sourceKey] + '尚未刷新')) emit(task, 'skipped', 'march7th.not_due', 'satisfied');
    for (const [key, pattern, status, rule] of rules)
      if (task.sourceKey === key && pattern.test(text)) emit(task, status, 'march7th.' + rule, status === 'skipped' ? 'satisfied' : undefined);
  }
  // Only aggregate this reviewed reward wrapper; unrelated parents may have their own work.
  const parent = tasks.find(t => t.sourceKey === 'reward_enable');
  const children = parent ? tasks.filter(t => t.parentId === parent.id && t.requiredForParent) : [];
  if (parent && children.length && children.every(t => ['succeeded', 'skipped'].includes(states[t.id]?.status))
      && !['succeeded', 'failed'].includes(states[parent.id]?.status))
    emit(parent, 'succeeded', 'march7th.reward.children_completed');

}

console.log(dispatch());
