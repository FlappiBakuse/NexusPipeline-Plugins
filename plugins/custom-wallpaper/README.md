# 自定义壁纸

`custom-wallpaper` v0.1.1 使用 NexusPipeline Frontend API 1.1 提供设置页卡片。壁纸文件和外观配置由宿主保存到当前实例，所有浏览器通过 revision 轮询获得相同的顺序、随机轮换和显示参数。

宿主会校验图片类型、文件魔数、单文件大小、数量和总容量，并在前端根据图片内容生成自适应强调色；启用壁纸后仍可使用宿主主题切换按钮。卡片与侧边栏透明度可在 0% 至 50% 之间调节，浅色、深色和跟随系统主题均保持有效。
