# 游戏自动签到

`game-checkin` v0.1.3 是原 `hoyolab-checkin` 的身份替换版本。它在用户脚本开始运行时按已选择的平台处理米游社官服或 HoYoLAB 国际服的原神、崩坏：星穹铁道和绝区零签到。

米游社与 HoYoLAB 使用独立 Cookie。米游社流程会先查询绑定角色和当日状态，再执行签到；返回验证码时记录“需要手动完成验证码”，当前轮次停止处理该账号，不调用第三方打码服务。

宿主更新到 v0.11.3 后，安装此插件会迁移旧插件的用户配置、密钥、作用域数据和启用状态；前端信任需要按新插件身份重新确认。
